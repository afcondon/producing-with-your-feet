[package]
name = "pwyf-looper"
version = "0.1.0"
edition = "2021"
description = "Looper engine for producing-with-your-feet: CoreAudio capture/playback, layered loops, and the latency metrology the layering depends on"

[dependencies]
cpal = "0.15"
tungstenite = "0.24"

[profile.release]
opt-level = 3
lto = "thin"
